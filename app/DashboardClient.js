'use client';
import { useEffect, useMemo, useState } from 'react';
import { airlineForMawb, CONFIGURED_PREFIXES } from '../lib/airlines.js';
import { normalizeExportTimesToIst } from '../lib/exportIst.js';

const KEY='mayavi_v3_shipments';
const TAB_KEY='mayavi_dashboard_active_tab';
const ONE_HOUR=60*60*1000;
const SERVER_POLL=60*1000;
function normalize(v=''){const d=String(v).replace(/\D/g,'');return d.length===11?`${d.slice(0,3)}-${d.slice(3)}`:''}
function digits(v=''){return String(v).replace(/\D/g,'')}
function pad(v){return String(v).padStart(2,'0')}
function cleanWeight(v=''){return String(v||'').replace(/\s*(kg|kgs|kilograms?)\s*$/i,'').trim()}
function shipmentTypeOf(value=''){const t=String(value||'').trim().toUpperCase();return t==='OTHER_COUNTRIES'?'OTHER_COUNTRIES':t==='EXPORT'?'EXPORT':'IMPORT'}
function isExportLikeType(value=''){const t=shipmentTypeOf(value);return t==='EXPORT'||t==='OTHER_COUNTRIES'}
function weightForTotal(row={}){const direct=Number(row.totalWeight);if(Number.isFinite(direct)&&direct>0)return direct;const s=cleanWeight(row.weight).replace(/,/g,'').trim();if(!s)return 0;const parts=s.split('/').map(x=>Number(String(x).replace(/[^0-9.\-]/g,''))).filter(Number.isFinite);return parts.length?(parts.length>1?parts.at(-1):parts[0]):0}
function weightMinusForTotal(row={}){const n=Number(String(row.weightMinus??'').replace(/,/g,'').trim());return Number.isFinite(n)&&n>=0?n:0}
function normalizeFlightNo(mawb='',value=''){const n=normalize(mawb),f=String(value||'').trim().replace(/\s+/g,'').toUpperCase();if(n.startsWith('065-')&&/^\d{1,4}$/.test(f))return`SV${f}`;return f}
function formatEntryDate(value=''){if(!value)return'';const d=new Date(value);if(!Number.isFinite(d.getTime()))return'';return `${pad(d.getDate())}/${pad(d.getMonth()+1)}/${d.getFullYear()}`}
function formatTime12(value=''){const s=String(value||'').trim();if(!s)return'';if(/\b(?:AM|PM)\b/i.test(s))return s.replace(/\b(am|pm)\b/i,m=>m.toUpperCase());const m=s.match(/^(\d{1,2}):([0-5]\d)$/);if(!m)return s;const h=Number(m[1]);if(h<0||h>23)return s;return `${pad(h%12||12)}:${m[2]} ${h>=12?'PM':'AM'}`}
function dateTimeValue(date='',time=''){const dm=String(date).match(/^(\d{4})-(\d{2})-(\d{2})$/),tm=String(time).match(/^(\d{1,2}):(\d{2})/);if(!dm||!tm)return null;const d=new Date(Number(dm[1]),Number(dm[2])-1,Number(dm[3]),Number(tm[1]),Number(tm[2]),0,0);return Number.isFinite(d.getTime())?d:null}
function previousDate(date=''){const m=String(date||'').match(/^(20\d{2})-(\d{2})-(\d{2})$/);if(!m)return false;const now=new Date(),today=`${now.getFullYear()}-${pad(now.getMonth()+1)}-${pad(now.getDate())}`;return String(date)<today}
function mailTimeFrom(date='',time=''){if(!String(date||'').trim()||!String(time||'').trim())return'';const d=dateTimeValue(date,time);if(!d)return'';d.setHours(d.getHours()-6);return `${d.getFullYear()}-${pad(d.getMonth()+1)}-${pad(d.getDate())} ${formatTime12(`${pad(d.getHours())}:${pad(d.getMinutes())}`)}`}
function handoverTimeFrom(date='',time=''){const d=dateTimeValue(date,time);if(!d)return'';d.setHours(d.getHours()-5);return `${d.getFullYear()}-${pad(d.getMonth()+1)}-${pad(d.getDate())} ${formatTime12(`${pad(d.getHours())}:${pad(d.getMinutes())}`)}`}
function departurePassed(row={}){const d=dateTimeValue(row.departureDate||row.flightDate||'',row.departureTime||'');return Boolean(d&&Date.now()>=d.getTime())}
function airportCode(value=''){const m=String(value||'').toUpperCase().match(/\b([A-Z]{3})\b/);return m?.[1]||''}
function explicitMovementStation(row={}){
  for(const value of [row.arrivalStation,row.arrivalAirport,row.currentLocation,row.lastLocation,row.eventStation,row.deliveryStation,row.airport,row.location]){
    const code=airportCode(value);if(code)return code;
  }
  return'';
}
function explicitViaForStatus(row={}){
  const origin=airportCode(row.origin),destination=airportCode(row.destination);
  for(const value of [row.via,row.transitAirport,row.routeVia,row.connectionAirport,row.hub,row.departureDestination]){
    const code=airportCode(value);if(code&&code!==origin&&code!==destination)return code;
  }
  return'';
}
function viaForRow(row={}){
  const origin=airportCode(row.origin),destination=airportCode(row.destination),n=normalize(row.mawb||row.awb||'');
  const explicit=[row.via,row.transitAirport,row.routeVia,row.connectionAirport,row.hub].map(airportCode).find(Boolean);
  if(explicit)return explicit;
  const departureDestination=airportCode(row.departureDestination);
  if(departureDestination&&departureDestination!==destination)return departureDestination;
  const airport=airportCode(row.airport);
  if(airport&&airport!==origin&&airport!==destination)return airport;
  const current=airportCode(row.currentLocation);
  if(current&&current!==origin&&current!==destination)return current;
  // Verified operating patterns requested for Mayavi display when the source does not expose a via field.
  if(n.startsWith('065-')&&origin==='DMM'&&destination&&destination!=='RUH')return'RUH';
  if(n.startsWith('910-')&&origin!=='MCT'&&destination&&destination!=='MCT')return'MCT';
  return origin||'';
}
function businessStatus(raw='',timingStatus='',arrivalDate='',mawb='',row={}){
  const s=String(raw||'').toUpperCase();
  const n=normalize(mawb);
  const via=viaForRow(row).toUpperCase();
  const origin=String(row.origin||'').toUpperCase(),destination=String(row.destination||'').toUpperCase();
  const directDelhi=destination==='DEL'&&!via;
  if(s.includes('PART ARRIVED'))return'PART ARRIVED';
  if(s.includes('PART DEPARTED')||s.includes('PART SHIPMENT')||s.includes('PART LOAD'))return'PART LOAD';
  if(s.includes('DELIVER'))return'DELIVERED';
  const arrivedSignal=s.includes('ARRIVED')||s.includes('DESTINATION')||s.includes('LANDED')||s.includes('RCF');
  if(arrivedSignal){
    const finalDestination=airportCode(row.destination);
    const movementStation=explicitMovementStation(row);
    const explicitVia=explicitViaForStatus(row);
    if(finalDestination&&((movementStation&&movementStation!==finalDestination)||(!movementStation&&explicitVia&&airportCode(row.departureDestination)===explicitVia)))return'IN TRANSIT';
    return'ARRIVED';
  }
  if(s.includes('DELAY')||s.includes('LATE'))return'DELAYED';
  // Emirates: a generic DEP can be an intermediate/via leg. Only the parser's
  // explicit final-leg departure should display DEPARTED; otherwise keep it IN TRANSIT.
  if(n.startsWith('176-')&&(s.includes('IN TRANSIT')||s==='DEP'||s.includes('DEPART')))return s==='DEPARTED'?'DEPARTED':'IN TRANSIT';
  if(s.includes('DEPART')||s.includes('AIRBORNE')||s.includes('IN FLIGHT')||s==='DEP')return'DEPARTED';
  if((n.startsWith('065-')||via==='RUH')&&(s.includes('FOW')||s.includes('FOH')||s.includes('TRANSIT')||s.includes('IN TRANSIT')))return'IN TRANSIT';
  if(s.includes('IN TRANSIT')||s.includes('TRANSIT'))return directDelhi?'DEPARTED':'IN TRANSIT';
  if(timingStatus==='EARLY'||s.includes('EARLY'))return'EARLY ARRIVAL';
  if(timingStatus==='DELAYED')return'DELAYED';
  if(s.includes('PRE-MANIFEST'))return'PRE-MANIFESTED';
  if(s.includes('MANIFEST')||s.includes('ACCEPT')||s.includes('BUILT')||s.includes('EXECUT')||s.includes('BOOK'))return'BOOKED';
  return'BOOKED';
}
function tone(status=''){const s=String(status).toUpperCase();if(s.includes('PART ARRIVED')||s.includes('PART LOAD'))return'transit';if(s.includes('DELIVER')||s.includes('ARRIVED'))return'arrived';if(s.includes('TRANSIT')||s.includes('DEPART')||s.includes('AIRBORNE')||s.includes('IN FLIGHT'))return'transit';if(s.includes('DELAY'))return'delayed';if(s.includes('EARLY'))return'early';return'booked'}
function trackingProgress(status=''){const s=String(status||'').toUpperCase();if(s.includes('DELIVER'))return 6;if(s.includes('ARRIVED')||s.includes('RCF')||s.includes('LANDED'))return 5;if(s.includes('DELAY'))return 4;if(s.includes('TRANSIT')||s.includes('DEPART')||s.includes('AIRBORNE')||s.includes('IN FLIGHT'))return 3;if(s.includes('BOOK')||s.includes('ACCEPT')||s.includes('MANIFEST'))return 2;return 0}
function recoverTurkishLocal(server={},local={}){
  const out={...server};let changed=false;
  const keys=['bookingDate','bookingTime','flightNo','flightDate','origin','destination','via','bags','pieces','weight','volume','departureDate','departureTime','departureFlightNo','departureOrigin','departureDestination','scheduledDeparture','scheduledArrivalDate','scheduledArrivalTime','arrivalDate','arrivalTime','actualArrivalDate','actualArrivalTime','actualArrival','deliveryStation','deliveryDate','deliveryTime','deliveredAt','arrivalTimeSource','departureTimeSource','source','provider'];
  for(const key of keys){
    const sv=out[key],lv=local?.[key];
    if((sv===''||sv===null||sv===undefined)&&lv!==''&&lv!==null&&lv!==undefined){out[key]=lv;changed=true}
  }
  if(local?.arrivalIsActual===true&&out.arrivalIsActual!==true){out.arrivalIsActual=true;changed=true}
  if(trackingProgress(local?.status)>trackingProgress(out.status)){out.status=local.status;changed=true}
  if(changed){
    out.trackingError='';
    out.manualHint='';
    out.localRecoveryAt=new Date().toISOString();
    out.source=out.source||'Recovered verified Turkish browser data';
  }
  return{row:out,changed};
}
function decorateTiming(existing={},incoming={}){
  const raw={...existing,...incoming};
  const shipmentType=shipmentTypeOf(raw.shipmentType);
  const exportLike=isExportLikeType(shipmentType);
  const arrivalChanged=Object.prototype.hasOwnProperty.call(incoming,'arrivalDate')||Object.prototype.hasOwnProperty.call(incoming,'arrivalTime');
  const departureChanged=Object.prototype.hasOwnProperty.call(incoming,'departureDate')||Object.prototype.hasOwnProperty.call(incoming,'departureTime')||Object.prototype.hasOwnProperty.call(incoming,'flightDate');
  const scheduledChanged=Object.prototype.hasOwnProperty.call(incoming,'scheduledArrivalDate')||Object.prototype.hasOwnProperty.call(incoming,'scheduledArrivalTime');
  const merged=normalizeExportTimesToIst({
    ...raw,
    shipmentType,
    arrivalTimeZone:arrivalChanged?(incoming.arrivalTimeZone||''):(raw.arrivalTimeZone||''),
    departureTimeZone:departureChanged?(incoming.departureTimeZone||''):(raw.departureTimeZone||''),
    scheduledArrivalTimeZone:scheduledChanged?(incoming.scheduledArrivalTimeZone||''):(raw.scheduledArrivalTimeZone||'')
  });
  const via=viaForRow(merged);

  const rowMawb=normalize(incoming.mawb||existing.mawb||existing.awb||'');
  const scheduledArrivalDate=merged.scheduledArrivalDate||merged.arrivalDate||'';
  const scheduledArrivalTime=merged.scheduledArrivalTime||merged.arrivalTime||'';
  const saudiaMawb=normalize(rowMawb).startsWith('065-');
  const saudiaFlightDate=String(merged.flightDate||'').match(/^(20\d{2}-\d{2}-\d{2})/)?.[1]||'';
  const hasIncomingArrivalDate=Object.prototype.hasOwnProperty.call(incoming,'arrivalDate');
  const arrivalDate=saudiaMawb
    ? (hasIncomingArrivalDate?(merged.arrivalDate||''):(saudiaFlightDate||merged.arrivalDate||''))
    : (hasIncomingArrivalDate?(merged.arrivalDate||''):(merged.arrivalDate||''));
  const arrivalTime=Object.prototype.hasOwnProperty.call(incoming,'arrivalTime')?(merged.arrivalTime||''):(merged.arrivalTime||'');

  const planned=dateTimeValue(scheduledArrivalDate,scheduledArrivalTime);
  const current=dateTimeValue(arrivalDate,arrivalTime);
  let timingDeltaMinutes=null,timingStatus='';
  if(planned&&current){
    timingDeltaMinutes=Math.round((current-planned)/60000);
    timingStatus=timingDeltaMinutes>60?'DELAYED':timingDeltaMinutes<-60?'EARLY':'ON TIME';
  }

  const rawStatus=incoming.status||existing.status||'';
  const status=businessStatus(rawStatus,timingStatus,arrivalDate,incoming.mawb||existing.mawb||existing.awb||'',merged);
  const wasDelayed=existing.wasDelayed===true||incoming.wasDelayed===true||String(existing.status||'').toUpperCase().includes('DELAY')||String(incoming.status||'').toUpperCase().includes('DELAY')||existing.timingStatus==='DELAYED'||incoming.timingStatus==='DELAYED'||timingStatus==='DELAYED';
  const movedAfterDelay=wasDelayed&&(status==='DEPARTED'||status==='IN TRANSIT'||status==='ARRIVED'||status==='PART ARRIVED');
  const remarks=movedAfterDelay
    ? (merged.remarks?String(merged.remarks).includes('Flight was delayed')?merged.remarks:`${merged.remarks} / Flight was delayed`:'Flight was delayed')
    : (merged.remarks||'');

  const hasIncomingFlight=Object.prototype.hasOwnProperty.call(incoming,'flightNo')||Object.prototype.hasOwnProperty.call(incoming,'flight');
  const flightNo=normalizeFlightNo(rowMawb,hasIncomingFlight?(incoming.flightNo||incoming.flight||''):(existing.flightNo||existing.flight||''));
  const bookingDate=incoming.bookingDate||existing.bookingDate||'';
  const bookingTime=incoming.bookingTime||existing.bookingTime||'';
  const mailArrivalDate=String(arrivalDate||'').split('/').map(x=>x.trim()).filter(Boolean).at(-1)||'';
  const mailTime=shipmentType==='IMPORT'?mailTimeFrom(mailArrivalDate,arrivalTime):'';
  const mailSent=shipmentType==='IMPORT'?merged.mailSent===true:undefined;
  const customsCleared=shipmentType==='IMPORT'?merged.customsCleared===true:undefined;
  const masterCopyReceived=exportLike?merged.masterCopyReceived===true:undefined;
  const departureDate=exportLike?(merged.departureDate||merged.flightDate||''):merged.departureDate;
  const departureTime=exportLike?(merged.departureTime||merged.etd||merged.estimatedDepartureTime||merged.scheduledDepartureTime||''):merged.departureTime;
  const handoverTime=exportLike?handoverTimeFrom(departureDate,departureTime):'';
  const handoverDone=exportLike?merged.handoverDone===true:undefined;

  return {...merged,via,flightNo,bookingDate,bookingTime,destination:merged.destination||'',shipmentType,departureDate,departureTime,handoverTime,handoverDone,scheduledArrivalDate,scheduledArrivalTime,arrivalDate,arrivalTime,arrivalIsActual:Boolean(merged.arrivalIsActual),timingDeltaMinutes,timingStatus,status,wasDelayed,remarks,mailTime,mailSent,customsCleared,masterCopyReceived};
}
function clientStyle(name=''){const s=String(name||'').trim();if(!s)return{};let h=0;for(let i=0;i<s.length;i++)h=(h*31+s.charCodeAt(i))%360;return {backgroundColor:`hsl(${h} 75% 92%)`,color:`hsl(${h} 48% 28%)`,borderColor:`hsl(${h} 55% 76%)`}}
function officialUrl(row={}){
  const n=normalize(row.mawb),airline=airlineForMawb(n),serial=n.slice(4),allDigits=digits(n);
  if(n.startsWith('157-'))return `https://www.qrcargo.com/s/track-your-shipment?documentNumber=${encodeURIComponent(serial)}&documentPrefix=157&documentType=MAWB`;
  if(n.startsWith('607-'))return `https://www.etihadcargo.com/en/e-services/track-shipment?awb=${encodeURIComponent(allDigits)}`;
  if(n.startsWith('235-'))return 'https://www.turkishcargo.com/en/cargo-tracking';
  if(n.startsWith('514-'))return 'https://airarabia-g9.ibsplc.aero/icargoneoportal/app/main/#/app';
  return airline?.url||row.officialTracker||'';
}
function officialHasDirectMawb(row={}){const n=normalize(row.mawb);return n.startsWith('157-')||n.startsWith('607-')}
function openOfficial(row={}){
  const n=normalize(row.mawb),url=officialUrl(row);if(!url)return;
  try{navigator.clipboard?.writeText(digits(n)).catch(()=>{})}catch{}
  window.open(url,'_blank','noopener,noreferrer');
}
function dbToRow(record={}){const d=record.data||{};return decorateTiming({}, {...d,mawb:normalize(d.mawb||d.awb||record.awb),clientName:d.clientName??d.client??'',companyType:d.companyType||'',companyName:d.companyName||'',airlineName:d.airlineName??d.airline??'',carrierCode:d.carrierCode??d.airlineCode??'',flightNo:d.flightNo??d.flight??'',pieces:d.pieces??d.bags??'',bags:d.bags??d.pieces??'',weight:cleanWeight(d.weight),goodsDescription:d.goodsDescription??d.goods_description??'',bookingDate:d.bookingDate||'',shipmentType:shipmentTypeOf(d.shipmentType),officialTracker:d.officialTracker||d.sourceUrl||'',enteredBy:d.enteredBy||'',enteredByUsername:d.enteredByUsername||'',enteredAt:d.enteredAt||'',mailSent:shipmentTypeOf(d.shipmentType)==='IMPORT'?d.mailSent===true:undefined,customsCleared:shipmentTypeOf(d.shipmentType)==='IMPORT'?d.customsCleared===true:undefined,masterCopyReceived:isExportLikeType(d.shipmentType)?d.masterCopyReceived===true:undefined,lastChecked:d.lastChecked||record.tracking_checked_at||record.updated_at||'',_dbUpdatedAt:record.updated_at||''})}
function withoutMeta(row={}){const {_dbUpdatedAt,...clean}=row;return clean}
function isCustomsArchived(row={}){return shipmentTypeOf(row.shipmentType)==='IMPORT'&&row.customsCleared===true}
function isExportArchived(row={}){return isExportLikeType(row.shipmentType)&&row.handoverDone===true&&row.masterCopyReceived===true}
function isPartArrived(row={}){return String(row.status||'').toUpperCase().includes('PART ARRIVED')}
function arrivalSortTime(row={}){const d=dateTimeValue(row.arrivalDate,row.arrivalTime);return d?d.getTime():Number.POSITIVE_INFINITY}
function mailSortTime(row={}){const dates=String(row.arrivalDate||'').split('/').map(x=>x.trim()).filter(Boolean);const date=dates.length>1?dates[dates.length-1]:dates[0]||'';const d=dateTimeValue(date,row.arrivalTime);if(!d)return Number.POSITIVE_INFINITY;d.setHours(d.getHours()-6);return d.getTime()}
function handoverSortTime(row={}){const d=dateTimeValue(row.departureDate||row.flightDate||'',row.departureTime||'');if(!d)return Number.POSITIVE_INFINITY;d.setHours(d.getHours()-5);return d.getTime()}
function priorityValue(row={}){const s=String(row.status||'').toUpperCase();if(s.includes('ARRIVED')&&!s.includes('PART ARRIVED'))return 0;if(s.includes('PART ARRIVED'))return 1;if(s.includes('IN TRANSIT')||s.includes('TRANSIT')||s.includes('DEPART')||s.includes('AIRBORNE')||s.includes('IN FLIGHT'))return 2;if(s.includes('DELAY'))return 3;return 4}
function sortPriority(list=[]){return [...list].sort((a,b)=>{const p=priorityValue(a)-priorityValue(b);if(p)return p;const at=arrivalSortTime(a),bt=arrivalSortTime(b);if(at!==bt)return at-bt;return (Date.parse(b.lastChecked||b._dbUpdatedAt||0)||0)-(Date.parse(a.lastChecked||a._dbUpdatedAt||0)||0)})}
function sortForDashboard(list=[],activeTab='IMPORT'){if(isExportLikeType(activeTab))return [...list].sort((a,b)=>{const ah=handoverSortTime(a),bh=handoverSortTime(b);if(ah!==bh)return ah-bh;return (Date.parse(b.lastChecked||b._dbUpdatedAt||0)||0)-(Date.parse(a.lastChecked||a._dbUpdatedAt||0)||0)});return [...list].sort((a,b)=>{const am=mailSortTime(a),bm=mailSortTime(b);if(am!==bm)return am-bm;const p=priorityValue(a)-priorityValue(b);if(p)return p;return (Date.parse(b.lastChecked||b._dbUpdatedAt||0)||0)-(Date.parse(a.lastChecked||a._dbUpdatedAt||0)||0)})}
function expandPartRows(list=[]){return list.flatMap(row=>{const prefix=digits(row.mawb).slice(0,3);if(prefix!=='065'&&prefix!=='098')return[row];const raw=Array.isArray(row.partShipments)?row.partShipments.filter(Boolean):[];const status=String(row.status||'').toUpperCase();const fractional=[row.bags,row.pieces,row.weight].some(v=>/^\s*[^/]+\/[^/]+\s*$/.test(String(v||'')));const genuinePartLoad=row.isPartLoad===true||status.includes('PART ARRIVED')||status.includes('PART LOAD')||status.includes('PART SHIPMENT')||fractional;if(!genuinePartLoad)return[row];const seen=new Set(),parts=[];for(const p of raw){const k=`${String(p.pieces||'').trim()}|${String(p.weight||'').replace(/,/g,'').trim()}|${String(p.flightNo||'').trim()}|${String(p.arrivalDate||'').trim()}|${String(p.arrivalTime||'').trim()}`;if(k==='||||'||seen.has(k))continue;seen.add(k);parts.push(p)}if(parts.length<2)return[row];return parts.map((p,idx)=>({...row,_partKey:`${digits(row.mawb)}::${idx}`,_partIndex:idx,bags:p.totalPieces?`${p.pieces}/${p.totalPieces}`:(p.pieces||row.bags),pieces:p.totalPieces?`${p.pieces}/${p.totalPieces}`:(p.pieces||row.pieces),weight:p.totalWeight?`${p.weight}/${p.totalWeight}`:(p.weight||row.weight),flightNo:p.flightNo||'',flightDate:p.flightDate||'',arrivalDate:p.arrivalDate||'',arrivalTime:p.arrivalTime||'',mailTime:mailTimeFrom(p.arrivalDate||'',p.arrivalTime||''),mailSent:p.mailSent===true,mailUpdatedAt:p.mailUpdatedAt||'',remarks:'Part Shipment'}))})}
function uniq(rows,key){return [...new Set(rows.map(r=>String(r?.[key]||'').trim()).filter(Boolean))].sort((a,b)=>a.localeCompare(b))}
function stickyColumn(left,width,header=false){return {position:'sticky',left,zIndex:header?8:4,minWidth:width,width,background:header?'#f8fafc':'#fff',boxShadow:'1px 0 0 #e2e8f0'}}
function companyChoice(row={}){const t=String(row.companyType||'').toUpperCase(),n=String(row.companyName||'').trim();if(t==='KCC'||t==='PV'||t==='OTHER')return t;if(n.toUpperCase()==='KCC')return'KCC';if(n.toUpperCase()==='PV')return'PV';return n?'OTHER':''}

export default function DashboardClient({isAdmin=false,currentUser=null,onLogout=null}){
  const [rows,setRows]=useState([]),[mawb,setMawb]=useState(''),[client,setClient]=useState(''),[busy,setBusy]=useState(false),[note,setNote]=useState(''),[loaded,setLoaded]=useState(false),[shared,setShared]=useState(false),[activeTab,setActiveTab]=useState('IMPORT');
  const [adminView,setAdminView]=useState('ACTIVE'),[clientFilter,setClientFilter]=useState(''),[originFilter,setOriginFilter]=useState(''),[destinationFilter,setDestinationFilter]=useState(''),[prefixFilter,setPrefixFilter]=useState('');
  const employeeName=String(currentUser?.displayName||'').trim(),employeeUsername=String(currentUser?.username||'').trim();
  const canWeightMinus=isAdmin;
  const privateTab=activeTab==='OTHER_COUNTRIES';
  const exportLikeTab=isExportLikeType(activeTab);
  async function persistRows(list,markEntry=false){const clean=list.filter(x=>normalize(x?.mawb)).map(withoutMeta);if(!clean.length)return[];const res=await fetch('/api/shipments',{method:'POST',credentials:'include',headers:{'content-type':'application/json'},body:JSON.stringify({rows:clean,markEntry})});const data=await res.json();if(!data.ok)throw new Error(data.error||'Shared database save failed.');return data.rows||[]}
  async function persistRow(row,markEntry=false){return persistRows([row],markEntry)}
  useEffect(()=>{try{const saved=localStorage.getItem(TAB_KEY);if(saved==='IMPORT'||saved==='EXPORT'||(isAdmin&&saved==='OTHER_COUNTRIES'))setActiveTab(saved)}catch{}},[isAdmin]);
  useEffect(()=>{try{localStorage.setItem(TAB_KEY,activeTab)}catch{}},[activeTab]);
  useEffect(()=>{let active=true;(async()=>{let local=[];try{local=JSON.parse(localStorage.getItem(KEY)||'[]').map(x=>decorateTiming({}, {...x,mawb:normalize(x?.mawb||x?.awb),shipmentType:shipmentTypeOf(x?.shipmentType)})).filter(x=>x.mawb&&(isAdmin||x.shipmentType!=='OTHER_COUNTRIES'))}catch{}try{const res=await fetch('/api/shipments',{cache:'no-store',credentials:'include'});const data=await res.json();if(!data.ok)throw new Error(data.error||'Shared database unavailable');const server=(data.rows||[]).map(dbToRow).filter(x=>x.mawb);const map=new Map(server.map(x=>[digits(x.mawb),x]));const migrate=[];for(const l of local){const key=digits(l.mawb),s=map.get(key);if(!s)continue;const lt=Date.parse(l.lastChecked||l._dbUpdatedAt||0)||0,st=Date.parse(s.lastChecked||s._dbUpdatedAt||0)||0;if(key.startsWith('235')){const recovered=recoverTurkishLocal(s,l);if(recovered.changed){map.set(key,recovered.row);migrate.push(recovered.row)}continue}const serverAuthoritative=key.startsWith('098');if(!serverAuthoritative&&lt>st){const mergedLocal={...l};for(const wk of ['shipmentType','clientName','client','companyType','companyName','goodsDescription','enteredBy','enteredByUsername','enteredAt','mailSent','mailUpdatedAt','customsCleared','customsClearedAt','customsClearedBy','masterCopyReceived','masterCopyReceivedAt','masterCopyReceivedBy','handoverDone','handoverUpdatedAt','handoverUpdatedBy','weightMinus','weightMinusUpdatedAt','weightMinusUpdatedBy']){if(Object.prototype.hasOwnProperty.call(s,wk))mergedLocal[wk]=s[wk];else delete mergedLocal[wk]}map.set(key,mergedLocal)}}const merged=[...map.values()].sort((a,b)=>(Date.parse(b.lastChecked||b._dbUpdatedAt||0)||0)-(Date.parse(a.lastChecked||a._dbUpdatedAt||0)||0));if(!active)return;setRows(merged);setShared(true);setLoaded(true);localStorage.setItem(KEY,JSON.stringify(merged.map(withoutMeta)));if(migrate.length)persistRows(migrate).catch(()=>{})}catch(e){if(!active)return;setRows(local);setLoaded(true);setShared(false);setNote(`Shared database unavailable — showing this browser backup only. ${e.message||''}`)}})();return()=>{active=false}},[isAdmin]);
  useEffect(()=>{if(typeof window!=='undefined'&&loaded)localStorage.setItem(KEY,JSON.stringify(rows.map(withoutMeta)))},[rows,loaded]);
  // Pull fresh rows from our own shared database once a minute. This does NOT
  // call airline websites; it only makes the open dashboard reflect tracking
  // results that the backend/another tab has already saved, so stale ETA/ATA
  // values cannot sit on screen until the one-hour page reload.
  useEffect(()=>{if(!loaded)return;let stopped=false,running=false;const syncServer=async()=>{if(stopped||running)return;running=true;try{
    const res=await fetch('/api/shipments',{cache:'no-store',credentials:'include'});
    const data=await res.json();if(!data.ok)return;
    const server=(data.rows||[]).map(dbToRow).filter(x=>x.mawb&&(isAdmin||x.shipmentType!=='OTHER_COUNTRIES'));
    setRows(current=>{
      const localMap=new Map(current.map(x=>[digits(x.mawb),x]));
      const out=[];
      for(const sr of server){
        const key=digits(sr.mawb),lr=localMap.get(key);localMap.delete(key);
        if(!lr){out.push(sr);continue}
        if(key.startsWith('235')){out.push(recoverTurkishLocal(sr,lr).row);continue}
        const st=Math.max(Date.parse(sr._dbUpdatedAt||0)||0,Date.parse(sr.lastChecked||0)||0);
        const lt=Math.max(Date.parse(lr._dbUpdatedAt||0)||0,Date.parse(lr.lastChecked||0)||0);
        out.push(key.startsWith('098')||st>=lt?sr:lr);
      }
      for(const lr of localMap.values())if(isAdmin||shipmentTypeOf(lr.shipmentType)!=='OTHER_COUNTRIES')out.push(lr);
      return out;
    });
    setShared(true);
  }catch{}finally{running=false}};syncServer();const id=setInterval(syncServer,SERVER_POLL);return()=>{stopped=true;clearInterval(id)}},[loaded,isAdmin]);
  useEffect(()=>{const id=setInterval(()=>window.location.reload(),ONE_HOUR);return()=>clearInterval(id)},[]);
  // Keep active masters fresh from their airline source; manual row refresh should not be required for status changes.
  useEffect(()=>{if(!loaded||!rows.length)return;let stopped=false,running=false;const syncActive=async()=>{if(stopped||running)return;running=true;try{const list=rows.filter(r=>!['ARRIVED','DELIVERED'].includes(String(r.status||'').toUpperCase())).sort((a,b)=>(a.shipmentType==='IMPORT'?0:1)-(b.shipmentType==='IMPORT'?0:1));for(const r of list){if(stopped)break;await refreshByMawb(r.mawb)}}finally{running=false}};syncActive();const id=setInterval(syncActive,10*60*1000);return()=>{stopped=true;clearInterval(id)}},[loaded,rows.length]);useEffect(()=>{setClientFilter('');setOriginFilter('');setDestinationFilter('');setPrefixFilter('')},[activeTab,adminView]);
  const tabRows=useMemo(()=>rows.filter(r=>shipmentTypeOf(r.shipmentType)===activeTab),[rows,activeTab]);
  const dashboardRows=useMemo(()=>{if(exportLikeTab){if(isAdmin&&adminView==='CLEARED')return tabRows.filter(isExportArchived);return tabRows.filter(r=>!isExportArchived(r))}if(isAdmin&&adminView==='CLEARED')return tabRows.filter(isCustomsArchived);return tabRows.filter(r=>!isCustomsArchived(r))},[tabRows,activeTab,isAdmin,adminView]);
  const clearedFilterMode=isAdmin&&adminView==='CLEARED';
  const customsFilterMode=clearedFilterMode&&activeTab==='IMPORT';
  const filterOptions=useMemo(()=>({clients:uniq(dashboardRows,'clientName'),origins:uniq(dashboardRows,'origin'),destinations:uniq(dashboardRows,'destination'),prefixes:[...new Set(dashboardRows.map(r=>digits(r.mawb).slice(0,3)).filter(Boolean))].sort()}),[dashboardRows]);
  const visibleRows=useMemo(()=>{const filtered=dashboardRows.filter(r=>(!prefixFilter||digits(r.mawb).startsWith(prefixFilter))&&(!clearedFilterMode||((!clientFilter||r.clientName===clientFilter)&&(!customsFilterMode||((!originFilter||r.origin===originFilter)&&(!destinationFilter||r.destination===destinationFilter))))));return sortForDashboard(expandPartRows(filtered),activeTab)},[dashboardRows,clearedFilterMode,customsFilterMode,clientFilter,originFilter,destinationFilter,prefixFilter,activeTab]);
  const totalWeight=useMemo(()=>visibleRows.reduce((sum,r)=>sum+weightForTotal(r),0),[visibleRows]);
  const totalWeightMinus=useMemo(()=>{const seen=new Set();return visibleRows.reduce((sum,r)=>{const key=digits(r.mawb);if(!key||seen.has(key))return sum;seen.add(key);return sum+weightMinusForTotal(r)},0)},[visibleRows]);
  const stats=useMemo(()=>({total:visibleRows.length,booked:visibleRows.filter(x=>x.status==='BOOKED'||x.status==='PRE-MANIFESTED').length,transit:visibleRows.filter(x=>x.status==='IN TRANSIT'||x.status==='PART ARRIVED').length,arrived:visibleRows.filter(x=>x.status==='ARRIVED'||x.status==='DELIVERED').length,attention:visibleRows.filter(x=>x.status==='DELAYED'||x.status==='EARLY ARRIVAL').length}),[visibleRows]);
  async function track(one,currentShipment={}){const n=normalize(one);if(!n)throw new Error('Enter valid 11-digit MAWB.');const fallback=normalize(n).startsWith('235-')?{flightNo:currentShipment.flightNo||'',flightDate:currentShipment.flightDate||'',departureDate:currentShipment.departureDate||'',departureTime:currentShipment.departureTime||'',origin:currentShipment.origin||'',destination:currentShipment.destination||'',departureFlightNo:currentShipment.departureFlightNo||'',departureOrigin:currentShipment.departureOrigin||'',departureDestination:currentShipment.departureDestination||'',scheduledDeparture:currentShipment.scheduledDeparture||''}:{};const res=await fetch('/api/track',{method:'POST',credentials:'include',headers:{'content-type':'application/json'},body:JSON.stringify({mawb:n,currentShipment:fallback})});const data=await res.json();if(!data.ok){const err=new Error(data.trackingError||data.apiError||data.error||'Tracking failed');err.payload=data;throw err}return {...data.shipment,provider:data.provider||data.shipment?.source||''}}
  async function importTurkishScreenshot(value,file){
    if(!file)return;
    const index=rows.findIndex(x=>normalize(x.mawb)===normalize(value)),row=rows[index];
    if(!row)return;
    setBusy(true);setNote(`Reading verified Turkish Cargo screenshot for ${row.mawb}…`);
    try{
      const form=new FormData();form.append('mawb',row.mawb);form.append('file',file);
      const res=await fetch('/api/turkish-screenshot',{method:'POST',credentials:'include',body:form});
      const data=await res.json();
      if(!data.ok)throw new Error(data.error||'Turkish screenshot could not be read.');
      const next=decorateTiming(row,{...data.shipment,shipmentType:row.shipmentType,clientName:row.clientName,companyType:row.companyType||'',companyName:row.companyName||'',goodsDescription:row.goodsDescription||'',enteredBy:row.enteredBy,enteredByUsername:row.enteredByUsername,enteredAt:row.enteredAt,mailSent:row.shipmentType==='IMPORT'?row.mailSent:undefined,mailUpdatedAt:row.shipmentType==='IMPORT'?(row.mailUpdatedAt||''):undefined,customsCleared:row.shipmentType==='IMPORT'?row.customsCleared===true:undefined,masterCopyReceived:isExportLikeType(row.shipmentType)?row.masterCopyReceived===true:undefined,lastChecked:new Date().toISOString(),trackingError:'',manualHint:''});
      setRows(list=>list.map((x,i)=>i===index?next:x));
      await persistRow(next);setShared(true);setNote(`${row.mawb}: Turkish verified screenshot fields filled and saved.`);
    }catch(e){setNote(`${row.mawb}: ${e.message||e}`)}
    finally{setBusy(false)}
  }
  async function add(){const n=normalize(mawb),clientName=String(client||'').trim();if(!n){setNote('Please enter valid 11-digit MAWB.');return}if(!clientName){setNote('Client Name is mandatory. Please enter the client name before adding the MAWB.');return}const airline=airlineForMawb(n);if(!airline){setNote(`Prefix ${n.slice(0,3)} is not mapped yet.`);return}const existing=rows.find(x=>normalize(x.mawb)===n);if(existing){setNote(`${n} is already fixed in ${existing.shipmentType||'IMPORT'} dashboard. Admin can move it if required.`);return}const enteredAt=new Date().toISOString();const base=decorateTiming({}, {mawb:n,shipmentType:activeTab,clientName,companyType:'',companyName:'',goodsDescription:activeTab==='IMPORT'?'':undefined,enteredBy:employeeName,enteredByUsername:employeeUsername,enteredAt,airlineName:airline.name,status:'BOOKED',officialTracker:airline.url||'',mailSent:activeTab==='IMPORT'?false:undefined,customsCleared:activeTab==='IMPORT'?false:undefined,masterCopyReceived:exportLikeTab?false:undefined,lastChecked:enteredAt});setRows(r=>[base,...r.filter(x=>normalize(x.mawb)!==n)]);setMawb('');setClient('');setBusy(true);setNote(`${n} added. Fetching live ${airline.name} details…`);try{await persistRow(base,true);setShared(true)}catch(e){setShared(false);setNote(`${n} added locally; shared save failed: ${e.message||e}`)}try{const s=await track(n);const next=decorateTiming(base,{...s,shipmentType:activeTab,clientName,companyType:base.companyType,companyName:base.companyName,goodsDescription:base.goodsDescription,enteredBy:employeeName,enteredByUsername:employeeUsername,enteredAt,mailSent:activeTab==='IMPORT'?false:undefined,customsCleared:activeTab==='IMPORT'?false:undefined,masterCopyReceived:exportLikeTab?base.masterCopyReceived===true:undefined,lastChecked:new Date().toISOString(),trackingError:'',manualHint:''});setRows(r=>r.map(x=>normalize(x.mawb)===n?next:x));try{await persistRow(next);setShared(true);setNote(`${n} live details filled and saved.`)}catch(e){setShared(false);setNote(`${n} live details filled locally; shared save failed: ${e.message||e}`)}}catch(e){const p=e.payload||{};const next=decorateTiming(base,{trackingError:e.message,manualHint:p.manualHint||'',officialTracker:airline.url||p.officialTracker||base.officialTracker,lastChecked:new Date().toISOString()});setRows(r=>r.map(x=>normalize(x.mawb)===n?next:x));try{await persistRow(next)}catch{}setNote(p.manualHint?`${n}: ${p.manualHint}`:`${n} saved. Live tracking will retry; use REFRESH if details stay blank.`)}finally{setBusy(false)}}
  async function refreshByMawb(value){const index=rows.findIndex(x=>normalize(x.mawb)===normalize(value)),row=rows[index];if(!row)return;setNote(`Refreshing ${row.mawb}…`);let next;try{const s=await track(row.mawb,row);let live={...s};const saudiaDis=normalize(row.mawb).startsWith('065-')&&(s.disAfterFow===true||/\bDIS\b|offloaded|not loaded/i.test(String(s.sourceStatus||'')+' '+String(s.discrepancy||'')+' '+String(s.remarks||'')));const saudiaLocked=normalize(row.mawb).startsWith('065-')&&row.saudiaExpectedArrivalLocked===true&&s.arrivalIsActual!==true&&!saudiaDis;if(saudiaDis){live={...s,flightNo:'',flightDate:'',arrivalDate:'',arrivalTime:'',arrivalIsActual:false,saudiaExpectedArrivalLocked:false,expectedDateOverrideSource:'',remarks:s.remarks||'Offloaded / not loaded to flight'};}else if(saudiaLocked){live={...s,flightDate:s.flightDate||row.flightDate,arrivalDate:s.arrivalDate||row.arrivalDate,arrivalTime:s.arrivalTime||row.arrivalTime,bookingDate:s.bookingDate||row.bookingDate,arrivalIsActual:false,saudiaExpectedArrivalLocked:true,expectedDateOverrideSource:s.arrivalDate?'SAL live expected arrival':(row.expectedDateOverrideSource||'Verified Saudia/SAL expected arrival')};}if(Array.isArray(live.partShipments)&&Array.isArray(row.partShipments)){live.partShipments=live.partShipments.map((p,i)=>{const id=String(p?.partId||`P${i+1}`);const saved=row.partShipments.find((x,j)=>String(x?.partId||`P${j+1}`)===id)||row.partShipments.find(x=>String(x?.pieces||'')===String(p?.pieces||'')&&String(x?.weight||'').replace(/,/g,'')===String(p?.weight||'').replace(/,/g,''));return saved&&Object.prototype.hasOwnProperty.call(saved,'mailSent')?{...p,mailSent:saved.mailSent===true,mailUpdatedAt:saved.mailUpdatedAt||''}:p;});}next=decorateTiming(row,{...live,shipmentType:row.shipmentType,clientName:row.clientName,companyType:row.companyType||'',companyName:row.companyName||'',goodsDescription:row.goodsDescription||'',enteredBy:row.enteredBy,enteredByUsername:row.enteredByUsername,enteredAt:row.enteredAt,mailSent:row.shipmentType==='IMPORT'?row.mailSent:undefined,mailUpdatedAt:row.shipmentType==='IMPORT'?(row.mailUpdatedAt||''):undefined,customsCleared:row.shipmentType==='IMPORT'?row.customsCleared===true:undefined,masterCopyReceived:isExportLikeType(row.shipmentType)?row.masterCopyReceived===true:undefined,lastChecked:new Date().toISOString(),trackingError:'',manualHint:''});if(normalize(row.mawb).startsWith('098-')){if(s.arrivalDate)next.arrivalDate=s.arrivalDate;if(s.arrivalTime)next.arrivalTime=s.arrivalTime;next.arrivalIsActual=s.arrivalIsActual===true;if(s.arrivalTimeZone)next.arrivalTimeZone=s.arrivalTimeZone;if(s.arrivalTimeSource)next.arrivalTimeSource=s.arrivalTimeSource;next.mailTime=next.shipmentType==='IMPORT'?mailTimeFrom(next.arrivalDate,next.arrivalTime):next.mailTime;}if(saudiaDis){next.flightNo='';next.flightDate='';next.arrivalDate='';next.arrivalTime='';next.mailTime='';next.arrivalIsActual=false;next.saudiaExpectedArrivalLocked=false;next.expectedDateOverrideSource='';next.remarks=live.remarks||'Offloaded / not loaded to flight';}else if(normalize(row.mawb).startsWith('065-')&&!s.flightNo&&!s.arrivalDate&&!s.arrivalTime){next.flightNo='';next.flightDate='';next.arrivalDate='';next.arrivalTime='';next.mailTime='';next.saudiaExpectedArrivalLocked=false;next.expectedDateOverrideSource='';}else if(s.arrivalIsActual===true){next.saudiaExpectedArrivalLocked=false;next.expectedDateOverrideSource='';}setRows(r=>r.map((x,i)=>i===index?next:x))}catch(e){const p=e.payload||{};const retained=decorateTiming(row,{status:row.status||'BOOKED',officialTracker:airlineForMawb(row.mawb)?.url||p.officialTracker||row.officialTracker,manualHint:p.manualHint||row.manualHint,trackingError:e.message,lastChecked:new Date().toISOString()});setRows(r=>r.map((x,i)=>i===index?retained:x));setNote(p.manualHint?`${row.mawb}: ${p.manualHint}`:'Auto refresh had an issue; last verified details and status were retained.');return}try{await persistRow(next);setShared(true);setNote(`${row.mawb} refreshed and shared.`)}catch(e){setShared(false);setNote(`${row.mawb} refreshed. Live details retained; shared save needs a valid login session.`)}}
  async function refreshAll(){setBusy(true);try{const masters=[...new Set(visibleRows.map(r=>normalize(r.mawb)).filter(Boolean))];for(const m of masters)await refreshByMawb(m)}finally{setBusy(false)}}
  async function setMail(value,sent,partKey=''){const index=rows.findIndex(x=>normalize(x.mawb)===normalize(value)),row=rows[index];if(!row||isExportLikeType(row.shipmentType))return;let next;if(partKey&&Array.isArray(row.partShipments)){const m=String(partKey).match(/::(\d+)$/),partIndex=m?Number(m[1]):-1;next={...row,partShipments:row.partShipments.map((p,i)=>i===partIndex?{...p,mailSent:sent,mailUpdatedAt:new Date().toISOString()}:p)};}else next={...row,mailSent:sent,mailUpdatedAt:new Date().toISOString()};setRows(r=>r.map((x,i)=>i===index?next:x));try{await persistRow(next);setShared(true);setNote(`${row.mawb}: Mail marked ${sent?'YES':'NO'} and saved.`)}catch(e){setShared(false);setNote(`Mail status save failed: ${e.message||e}`)}}
  async function setCustomsClear(value,cleared){const index=rows.findIndex(x=>normalize(x.mawb)===normalize(value)),row=rows[index];if(!row||isExportLikeType(row.shipmentType))return;const next={...row,customsCleared:cleared,customsClearedAt:cleared?new Date().toISOString():'',customsClearedBy:cleared?employeeName:''};setRows(r=>r.map((x,i)=>i===index?next:x));try{await persistRow(next);setShared(true);if(cleared)setNote(`${row.mawb}: Customs cleared — removed from Active Masters and kept in Admin Customs Cleared.`);else setNote(`${row.mawb}: Customs clear mark removed; master returned to Active Masters.`)}catch(e){setShared(false);setNote(`Customs clear status save failed: ${e.message||e}`)}}
  async function setHandover(value,done){const index=rows.findIndex(x=>normalize(x.mawb)===normalize(value)),row=rows[index];if(!row||!isExportLikeType(row.shipmentType))return;const next={...row,handoverDone:done,handoverUpdatedAt:new Date().toISOString(),handoverUpdatedBy:employeeName||'User'};setRows(r=>r.map((x,i)=>i===index?next:x));try{await persistRow(next);setShared(true);setNote(`${row.mawb}: Handover marked ${done?'YES':'NO'} and saved.`)}catch(e){setRows(r=>r.map((x,i)=>i===index?row:x));setShared(false);setNote(`Handover status save failed: ${e.message||e}`)}}
  async function setMasterCopyReceived(value,received){const index=rows.findIndex(x=>normalize(x.mawb)===normalize(value)),row=rows[index];if(!row||!isExportLikeType(row.shipmentType))return;const next={...row,masterCopyReceived:received,masterCopyReceivedAt:received?new Date().toISOString():'',masterCopyReceivedBy:received?(employeeName||'User'):''};setRows(r=>r.map((x,i)=>i===index?next:x));try{await persistRow(next);setShared(true);const archived=next.handoverDone===true&&next.masterCopyReceived===true;setNote(`${row.mawb}: Master Copy Received ${received?'ticked':'unticked'} and saved.${archived?(shipmentTypeOf(row.shipmentType)==='OTHER_COUNTRIES'?' Handover YES + Master Copy tick complete; moved to Other Countries → Customs Cleared.':' Handover YES + Master Copy tick complete; moved to Export Customs Cleared masters.'):(shipmentTypeOf(row.shipmentType)==='OTHER_COUNTRIES'?' It will stay in Other Countries → Active until both Handover YES and Master Copy tick are complete.':' It will stay in Active Export until both Handover YES and Master Copy tick are complete.')}`)}catch(e){setRows(r=>r.map((x,i)=>i===index?row:x));setShared(false);setNote(`Master Copy Received save failed: ${e.message||e}`)}}
  async function saveWeightMinus(value,weightMinus){if(!canWeightMinus)return;const index=rows.findIndex(x=>normalize(x.mawb)===normalize(value)),row=rows[index];if(!row||!isExportLikeType(row.shipmentType))return;const raw=String(weightMinus??'').trim();if(raw!==''&&(!Number.isFinite(Number(raw))||Number(raw)<0)){setNote('Weight Minus must be a positive number or blank.');return}const clean=raw===''?'':String(Number(raw));const next={...row,weightMinus:clean,weightMinusUpdatedAt:new Date().toISOString(),weightMinusUpdatedBy:employeeName||employeeUsername||'User'};setRows(r=>r.map((x,i)=>i===index?next:x));try{await persistRow(next);setShared(true);setNote(`${row.mawb}: Weight Minus ${clean?`${clean} kg`:'cleared'} and saved.`)}catch(e){setRows(r=>r.map((x,i)=>i===index?row:x));setShared(false);setNote(`Weight Minus save failed: ${e.message||e}`)}}
  async function saveGoodsDescription(value,goodsDescription){const index=rows.findIndex(x=>normalize(x.mawb)===normalize(value)),row=rows[index];if(!row||isExportLikeType(row.shipmentType))return;const next={...row,goodsDescription:String(goodsDescription||'').trim(),goodsDescriptionUpdatedAt:new Date().toISOString(),goodsDescriptionUpdatedBy:employeeName||'Admin'};setRows(r=>r.map((x,i)=>i===index?next:x));try{await persistRow(next);setShared(true);setNote(`${row.mawb}: Goods saved.`)}catch(e){setShared(false);setNote(`Goods save failed: ${e.message||e}`)}}
  async function saveCompany(value,companyType,companyName=''){const index=rows.findIndex(x=>normalize(x.mawb)===normalize(value)),row=rows[index];if(!row)return;const type=String(companyType||'').toUpperCase();const name=type==='KCC'?'KCC':type==='PV'?'PV':type==='OTHER'?String(companyName||'').trim():'';const next={...row,companyType:type,companyName:name,companyUpdatedAt:new Date().toISOString(),companyUpdatedBy:employeeName||'User'};setRows(r=>r.map((x,i)=>i===index?next:x));try{await persistRow(next);setShared(true);setNote(`${row.mawb}: Company ${name||type||'cleared'} saved.`)}catch(e){setShared(false);setNote(`Company save failed: ${e.message||e}`)}}
  async function editClientName(value){if(!isAdmin)return;const index=rows.findIndex(x=>normalize(x.mawb)===normalize(value)),row=rows[index];if(!row)return;const current=String(row.clientName||'').trim();const entered=window.prompt(`Change client name for ${row.mawb}`,current);if(entered===null)return;const clientName=String(entered||'').trim();if(!clientName){setNote('Client name cannot be blank.');return}if(clientName===current){setNote(`${row.mawb}: Client name unchanged.`);return}const next={...row,clientName,client:clientName,clientUpdatedAt:new Date().toISOString(),clientUpdatedBy:employeeName||'Admin'};setRows(r=>r.map((x,i)=>i===index?next:x));try{await persistRow(next);setShared(true);setNote(`${row.mawb}: Client changed from ${current||'—'} to ${clientName} by admin.`)}catch(e){setRows(r=>r.map((x,i)=>i===index?row:x));setShared(false);setNote(`Client name change failed: ${e.message||e}`)}}
  async function moveShipment(value){const index=rows.findIndex(x=>normalize(x.mawb)===normalize(value)),row=rows[index];if(!row)return;if(shipmentTypeOf(row.shipmentType)==='OTHER_COUNTRIES'){setNote(`${row.mawb} is protected in Other Countries and cannot be moved to an employee-visible tab.`);return}const target=isExportLikeType(row.shipmentType)?'IMPORT':'EXPORT';const next=decorateTiming(row,{...row,shipmentType:target,mailSent:target==='IMPORT'?false:undefined,mailUpdatedAt:target==='IMPORT'?row.mailUpdatedAt:undefined,customsCleared:target==='IMPORT'?false:undefined,customsClearedAt:target==='IMPORT'?'':undefined,customsClearedBy:target==='IMPORT'?'':undefined,masterCopyReceived:target==='EXPORT'?false:undefined,masterCopyReceivedAt:target==='EXPORT'?'':undefined,masterCopyReceivedBy:target==='EXPORT'?'':undefined,lastChecked:row.lastChecked||new Date().toISOString()});setRows(r=>r.map((x,i)=>i===index?next:x));try{await persistRow(next);setShared(true);setNote(`${row.mawb} moved to ${target}.`)}catch(e){setShared(false);setNote(`Could not move shipment: ${e.message||e}`)}}
  async function remove(value){if(!isAdmin)return;const index=rows.findIndex(x=>normalize(x.mawb)===normalize(value)),row=rows[index];if(!row)return;if(!window.confirm(`Delete ${row.mawb} from the shared tracker?`))return;try{const res=await fetch(`/api/shipments?awb=${encodeURIComponent(row.mawb)}`,{method:'DELETE',credentials:'include'});const data=await res.json();if(!data.ok)throw new Error(data.error||'Delete failed');setRows(r=>r.filter((_,i)=>i!==index));setShared(true);setNote(`${row.mawb} deleted by admin.`)}catch(e){setNote(`Delete blocked: ${e.message||e}`)}}
  const serialWidth=52,mawbWidth=132,goodsWidth=100,clientWidth=118,companyWidth=92;
  const mawbLeft=serialWidth,goodsLeft=serialWidth+mawbWidth,clientLeft=activeTab==='IMPORT'?serialWidth+mawbWidth+goodsWidth:serialWidth+mawbWidth,companyLeft=clientLeft+clientWidth;
  return <main>
    <section className="hero"><div><div className="eyebrow">MAYAVI CARGO • V4.3 • {isAdmin?'ADMIN':'EMPLOYEE'}</div><h1>{isAdmin?'Admin MAWB Dashboard':'Employee MAWB Dashboard'}</h1><p>Backend reload every 1 hour • Active tracking sync every 10 minutes with Import first • Import masters are ordered by Mail Time ascending • live status automatically changes to Booked, Pre-Manifested, In Transit, Part Arrived, Arrived, Delayed or Early Arrival • every new MAWB records the employee who entered it.</p></div><div className="userPanel"><div className="version">{employeeName||'User'} • {shared?'SHARED ✓':'LOCAL'}</div>{onLogout&&<button className="logoutBtn" onClick={onLogout}>LOGOUT</button>}</div></section>
    {isAdmin&&<section className="adminBar"><div><b>ADMIN ACCESS</b><span>Choose Active Masters, Customs Cleared, or the private Other Countries section. Other Countries has its own Active and Customs Cleared views and is visible only to Admin. PART ARRIVED import masters remain visible in both until fully arrived.</span></div></section>}
    {isAdmin?<section className="adminNavGroup"><section className="adminViews"><button className={adminView==='ACTIVE'&&!privateTab?'active':''} onClick={()=>{if(privateTab)setActiveTab('IMPORT');setAdminView('ACTIVE')}}>ACTIVE MASTERS</button><button className={adminView==='CLEARED'&&!privateTab?'active':''} onClick={()=>{if(privateTab)setActiveTab('IMPORT');setAdminView('CLEARED')}}>CUSTOMS CLEARED ({privateTab?0:(activeTab==='IMPORT'?tabRows.filter(isCustomsArchived).length:tabRows.filter(isExportArchived).length)})</button><button className={privateTab?'active':''} onClick={()=>{setAdminView('ACTIVE');setActiveTab('OTHER_COUNTRIES')}}>OTHER COUNTRIES</button></section>{privateTab?<section className="typeTabs"><button className={adminView==='ACTIVE'?'active':''} onClick={()=>setAdminView('ACTIVE')}>ACTIVE ({tabRows.filter(r=>!isExportArchived(r)).length})</button><button className={adminView==='CLEARED'?'active':''} onClick={()=>setAdminView('CLEARED')}>CUSTOMS CLEARED ({tabRows.filter(isExportArchived).length})</button></section>:<section className="typeTabs"><button className={activeTab==='IMPORT'?'active':''} onClick={()=>setActiveTab('IMPORT')}>IMPORT</button><button className={activeTab==='EXPORT'?'active':''} onClick={()=>setActiveTab('EXPORT')}>EXPORT</button></section>}</section>:<section className="typeTabs"><button className={activeTab==='IMPORT'?'active':''} onClick={()=>setActiveTab('IMPORT')}>IMPORT</button><button className={activeTab==='EXPORT'?'active':''} onClick={()=>setActiveTab('EXPORT')}>EXPORT</button></section>}
    <section className="stats"><div><b>{stats.total}</b><span>{isAdmin&&adminView==='CLEARED'?'Cleared':activeTab} MAWB</span></div><div><b>{stats.booked}</b><span>Booked</span></div><div><b>{stats.transit}</b><span>In Transit</span></div><div><b>{stats.arrived}</b><span>Arrived</span></div><div><b>{stats.attention}</b><span>Delayed / Early</span></div></section>
    {(!isAdmin||adminView==='ACTIVE')&&<section className="entry"><div><label>{privateTab?'OTHER COUNTRIES':activeTab} MAWB NUMBER</label><input value={mawb} onChange={e=>setMawb(e.target.value)} placeholder="e.g. 157-12345678" onKeyDown={e=>e.key==='Enter'&&add()}/></div><div><label>CLIENT NAME *</label><input required value={client} onChange={e=>setClient(e.target.value)} placeholder="Required client name" onKeyDown={e=>e.key==='Enter'&&add()}/></div><button disabled={busy} onClick={add}>{busy?'TRACKING…':`ADD TO ${privateTab?'OTHER COUNTRIES':activeTab}`}</button><button className="secondary" disabled={busy||!visibleRows.length} onClick={refreshAll}>REFRESH {activeTab}</button></section>}
    {note&&<div className="note">{note}</div>}
    <section className="filters"><div><label>MAWB PREFIX / AIRLINE</label><select value={prefixFilter} onChange={e=>setPrefixFilter(e.target.value)}><option value="">All Airlines</option>{filterOptions.prefixes.map(p=>{const sample=dashboardRows.find(r=>digits(r.mawb).startsWith(p));return <option key={p} value={p}>{p}{sample?.airlineName?` - ${sample.airlineName}`:''}</option>})}</select></div>{customsFilterMode&&<><div><label>ORIGIN</label><select value={originFilter} onChange={e=>setOriginFilter(e.target.value)}><option value="">All Origins</option>{filterOptions.origins.map(v=><option key={v} value={v}>{v}</option>)}</select></div><div><label>DESTINATION</label><select value={destinationFilter} onChange={e=>setDestinationFilter(e.target.value)}><option value="">All Destinations</option>{filterOptions.destinations.map(v=><option key={v} value={v}>{v}</option>)}</select></div></>}{(prefixFilter||clientFilter||originFilter||destinationFilter)&&<button onClick={()=>{setPrefixFilter('');setClientFilter('');setOriginFilter('');setDestinationFilter('')}}>CLEAR FILTERS</button>}</section>
    <section className="tableWrap"><table><thead><tr><th style={stickyColumn(0,serialWidth,true)}>S.No.</th>{(!isAdmin||adminView==='ACTIVE')&&<th style={{minWidth:'82px',width:'82px'}}>Entry Date</th>}<th style={stickyColumn(mawbLeft,mawbWidth,true)}>MAWB</th>{activeTab==='IMPORT'&&<th style={stickyColumn(goodsLeft,goodsWidth,true)}>Goods</th>}<th style={stickyColumn(clientLeft,clientWidth,true)}>{clearedFilterMode?<div style={{display:'grid',gap:'4px',minWidth:'112px'}}><span>Client</span><select value={clientFilter} onChange={e=>setClientFilter(e.target.value)} style={{height:'30px',border:'1px solid #cbd5e1',borderRadius:'7px',background:'#fff',padding:'0 5px',fontSize:'11px',fontWeight:700,color:'#475569'}}><option value="">All Clients</option>{filterOptions.clients.map(v=><option key={v} value={v}>{v}</option>)}</select></div>:'Client'}</th><th style={stickyColumn(companyLeft,companyWidth,true)}>Company</th><th>Entered By</th><th>Booking Date</th><th>Airline Name</th><th>Origin</th><th>Via</th><th>Destination</th><th style={{minWidth:'76px',width:'76px',paddingLeft:'8px',paddingRight:'1px'}}>Bags/Pieces</th><th style={{minWidth:'58px',width:'58px',paddingLeft:'1px',paddingRight:'8px'}}>Weight</th>{exportLikeTab&&canWeightMinus&&<th style={{minWidth:'100px',width:'100px'}}>Weight Minus</th>}<th>Flight</th>{exportLikeTab&&<><th>Departure Date</th><th>Departure Time</th><th>Handover Date & Time (-5h)</th><th>Handover</th></>}{activeTab==='IMPORT'&&<><th>Arrival Date</th><th>Arrival Time</th><th>Mail Time (-6h)</th><th>Mail</th><th>Customs Clear</th></>}{exportLikeTab&&(!isAdmin||adminView==='ACTIVE')&&<th>Master Copy Received</th>}{exportLikeTab&&<><th>Arrival Date</th><th>Arrival Time</th></>}<th>Status</th><th>Action</th>{activeTab==='IMPORT'&&<th style={{minWidth:'110px',width:'110px'}}>Remarks</th>}</tr></thead><tbody>{visibleRows.length?visibleRows.map((r,rowIndex)=><tr key={r._partKey||r.mawb}><td style={stickyColumn(0,serialWidth)}><strong>{rowIndex+1}</strong></td>{(!isAdmin||adminView==='ACTIVE')&&<td style={{minWidth:'82px',width:'82px',whiteSpace:'nowrap'}}>{formatEntryDate(r.enteredAt)||'—'}</td>}<td style={stickyColumn(mawbLeft,mawbWidth)}><strong>{r.mawb}</strong>{r.trackingError&&<small className="err">Backend will retry automatically</small>}</td>{activeTab==='IMPORT'&&<td style={stickyColumn(goodsLeft,goodsWidth)}>{isAdmin?<input value={r.goodsDescription||''} onChange={e=>{const value=e.target.value;setRows(list=>list.map(x=>normalize(x.mawb)===normalize(r.mawb)?{...x,goodsDescription:value}:x))}} onBlur={e=>saveGoodsDescription(r.mawb,e.target.value)} placeholder="Goods" style={{width:'88px',height:'32px',border:'1px solid #cbd5e1',borderRadius:'7px',padding:'0 6px',background:'#fff',fontWeight:/\b(PROTEIN|CLOTHES)\b/i.test(String(r.goodsDescription||''))?800:400,color:/\bPROTEIN\b/i.test(String(r.goodsDescription||''))?'#7e22ce':/\bCLOTHES\b/i.test(String(r.goodsDescription||''))?'#db2777':'#111827'}}/>:(r.goodsDescription?<span style={/\b(PROTEIN|CLOTHES)\b/i.test(String(r.goodsDescription||''))?{color:/\bPROTEIN\b/i.test(String(r.goodsDescription||''))?'#7e22ce':'#db2777',fontWeight:800}:undefined}>{r.goodsDescription}</span>:'—')}</td>}<td style={stickyColumn(clientLeft,clientWidth)} onClick={()=>isAdmin&&editClientName(r.mawb)} title={isAdmin?'Click client name to edit':''}>{r.clientName?<span className="clientChip" style={clientStyle(r.clientName)}>{r.clientName}</span>:'—'}</td><td style={stickyColumn(companyLeft,companyWidth)}><div style={{display:'grid',gap:'2px',minWidth:'82px'}}><select value={companyChoice(r)} onChange={e=>{const type=e.target.value;if(type==='OTHER'){setRows(list=>list.map(x=>normalize(x.mawb)===normalize(r.mawb)?{...x,companyType:'OTHER',companyName:companyChoice(x)==='OTHER'?(x.companyName||''):''}:x));saveCompany(r.mawb,'OTHER',companyChoice(r)==='OTHER'?r.companyName:'')}else saveCompany(r.mawb,type,type)}} style={{width:'82px',height:'28px',border:'1px solid #cbd5e1',borderRadius:'7px',background:'#fff',padding:'0 5px'}}><option value="">Select</option><option value="KCC">KCC</option><option value="PV">PV</option><option value="OTHER">Other</option></select>{companyChoice(r)==='OTHER'&&<input value={r.companyName||''} onChange={e=>{const value=e.target.value;setRows(list=>list.map(x=>normalize(x.mawb)===normalize(r.mawb)?{...x,companyType:'OTHER',companyName:value}:x))}} onBlur={e=>saveCompany(r.mawb,'OTHER',e.target.value)} placeholder="Type company" style={{width:'82px',height:'28px',border:'1px solid #cbd5e1',borderRadius:'7px',padding:'0 6px',background:'#fff'}}/>}</div></td><td>{r.enteredBy?<span className="employeeChip">{r.enteredBy}</span>:'—'}</td><td>{r.bookingDate||'—'}</td><td>{r.airlineName||'—'}</td><td>{r.origin||'—'}</td><td><strong>{viaForRow(r)||'—'}</strong></td><td>{r.destination||'—'}</td><td style={{minWidth:'76px',width:'76px',paddingLeft:'8px',paddingRight:'1px'}}>{r.bags||r.pieces||'—'}</td><td style={{minWidth:'58px',width:'58px',paddingLeft:'1px',paddingRight:'8px'}}>{r.weight?`${(String(r.mawb||'').replace(/\D/g,'').startsWith('098')&&String(r.weight).indexOf('/')<0&&r.masterWeight&&Number(r.masterWeight)>Number(String(r.weight).replace(/[^0-9.]/g,'')))?`${r.weight}/${r.masterWeight}`:r.weight} kg`:'—'}</td>{exportLikeTab&&canWeightMinus&&<td style={{minWidth:'100px',width:'100px'}}><div style={{display:'flex',alignItems:'center',gap:'4px'}}><input type="number" min="0" step="0.01" value={r.weightMinus??''} onChange={e=>{const value=e.target.value;setRows(list=>list.map(x=>normalize(x.mawb)===normalize(r.mawb)?{...x,weightMinus:value}:x))}} onBlur={e=>saveWeightMinus(r.mawb,e.target.value)} placeholder="0" style={{width:'68px',height:'32px',border:'1px solid #cbd5e1',borderRadius:'7px',padding:'0 6px',background:'#fff',fontWeight:700}}/><span style={{fontSize:'11px',fontWeight:700}}>kg</span></div></td>}<td>{normalizeFlightNo(r.mawb,r.flightNo)||'—'}</td>{exportLikeTab&&<><td>{r.departureDate||r.flightDate||'—'}</td><td>{formatTime12(r.departureTime)||'—'}</td><td><strong className="mailTime">{r.handoverTime||handoverTimeFrom(r.departureDate||r.flightDate,r.departureTime)||'—'}</strong></td><td><div className="mailChoice"><button className={`mailBtn yes ${r.handoverDone===true?'selected':''}`} onClick={()=>setHandover(r.mawb,true)}>YES</button><button className={`mailBtn no ${r.handoverDone!==true?'selected':''} ${r.handoverDone!==true&&departurePassed(r)?'urgentBlink':''}`} onClick={()=>setHandover(r.mawb,false)}>NO</button></div></td></>}{activeTab==='IMPORT'&&<><td>{r.arrivalDate||'—'}</td><td>{formatTime12(r.arrivalTime)||'—'}</td><td><strong className="mailTime">{r.mailTime||mailTimeFrom(r.arrivalDate,r.arrivalTime)||'—'}</strong></td><td><div className="mailChoice"><button className={`mailBtn yes ${r.mailSent===true?'selected':''}`} onClick={()=>setMail(r.mawb,true,r._partKey||'')}>YES</button><button className={`mailBtn no ${r.mailSent!==true?'selected':''}`} onClick={()=>setMail(r.mawb,false,r._partKey||'')}>NO</button></div></td><td><label className={`customsToggle ${r.customsCleared?'checked':''}`}><input type="checkbox" checked={r.customsCleared===true} onChange={e=>setCustomsClear(r.mawb,e.target.checked)}/><span>{r.customsCleared?'CLEARED':'PENDING'}</span></label></td></>}{exportLikeTab&&(!isAdmin||adminView==='ACTIVE')&&<td><label className={`customsToggle ${r.masterCopyReceived?'checked':''}`}><input type="checkbox" checked={r.masterCopyReceived===true} onChange={e=>setMasterCopyReceived(r.mawb,e.target.checked)}/><span>{r.masterCopyReceived?'✓':'TICK'}</span></label></td>}{exportLikeTab&&<><td>{r.arrivalDate||'—'}</td><td>{formatTime12(r.arrivalTime)||'—'}</td></>}<td><span className={`badge ${tone(r.status)}`}>{businessStatus(r.status,r.timingStatus,r.arrivalDate,r.mawb,r)}</span></td><td><div className="actions"><button className="refreshBtn" onClick={()=>refreshByMawb(r.mawb)}>REFRESH</button>{officialUrl(r)&&<a className={`trackLink ${r.trackingError?'urgent':''}`} href={officialUrl(r)} target="_blank" rel="noreferrer" onClick={e=>{e.preventDefault();openOfficial(r);setNote(officialHasDirectMawb(r)?`${r.mawb} opened directly on official tracking.`:`${r.mawb} copied and official tracking opened.`)}}>OFFICIAL TRACK ↗</a>}{!privateTab&&<button className="moveBtn" onClick={()=>moveShipment(r.mawb)}>MOVE TO {activeTab==='IMPORT'?'EXPORT':'IMPORT'}</button>}{isAdmin&&<button className="removeBtn" onClick={()=>remove(r.mawb)}>DELETE</button>}</div></td>{activeTab==='IMPORT'&&<td style={{minWidth:'110px',width:'110px',maxWidth:'130px',whiteSpace:'normal',fontSize:'11px'}}><div style={{minHeight:'30px',boxSizing:'border-box',width:'100%',padding:'5px 7px',border:'1px solid #cbd5e1',borderRadius:'6px',background:'#fff',fontWeight:r.remarks?700:400,lineHeight:1.25}}>{r.remarks||''}</div></td>}</tr>):<tr><td colSpan={activeTab==='IMPORT'?((!isAdmin||adminView==='ACTIVE')?23:22):((!isAdmin||adminView==='ACTIVE')?(canWeightMinus?24:23):(canWeightMinus?22:21))} className="empty">{isAdmin&&adminView==='CLEARED'?(activeTab==='IMPORT'?'No customs-cleared masters match these filters.':'No export masters with Master Copy Received yet.'):`No ${activeTab.toLowerCase()} MAWB added yet.`}</td></tr>}</tbody>{exportLikeTab&&canWeightMinus&&visibleRows.length>0&&<tfoot><tr><td colSpan={adminView==='ACTIVE'?13:12}></td><td style={{minWidth:'100px',width:'100px',fontWeight:900,whiteSpace:'nowrap'}}>Total: {totalWeightMinus.toLocaleString(undefined,{maximumFractionDigits:2})} kg</td><td colSpan={adminView==='ACTIVE'?10:9}></td></tr></tfoot>}</table></section>
    {(customsFilterMode||(isAdmin&&exportLikeTab))&&<section className="tableSummary"><span>{visibleRows.length} master{visibleRows.length===1?'':'s'} shown</span><strong>Total Weight: {totalWeight.toLocaleString(undefined,{maximumFractionDigits:2})} kg</strong>{isAdmin&&exportLikeTab&&<strong>Weight Minus: {totalWeightMinus.toLocaleString(undefined,{maximumFractionDigits:2})} kg</strong>}</section>}
    <footer>{CONFIGURED_PREFIXES.length} airline prefixes • Shared Neon storage • Import Mail Time ascending • Import Customs Clear archive • Export / Other Countries Master Copy Received archive • Admin-only private Other Countries • 1-hour backend reload • 10-minute active sync</footer>
  </main>
}